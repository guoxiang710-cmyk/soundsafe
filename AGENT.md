# SoundSafe 官网开发协作规范

## 1. 任务目标

在当前目录中开发一个用于互联网+创新创业大赛、大创答辩和项目路演的 SoundSafe 单页响应式官网。

项目必须：

- 使用 React + Vite + Tailwind CSS。
- 使用 lucide-react 提供界面图标。
- 可使用 framer-motion 实现轻量动画。
- 不依赖后端、数据库或远程业务接口。
- 采用深色科技风、AI 感知风和智慧安防风。
- 完整实现 `design.md` 中定义的模块、内容与响应式要求。

## 2. 开发前必读

开始修改前先阅读：

1. `design.md`
2. `AGENT.md`
3. `SKLL.md`
4. `package.json`
5. `src/` 中现有组件与样式

如果仓库已有实现，以现有结构和依赖为基础迭代，不无故重建项目或替换技术方案。

## 3. 实施原则

### 3.1 内容完整

- 不删减需求中的核心模块、数据、技术字段和指标说明。
- 页面文案使用中文，模型名、字段名和数据结构保留英文。
- 不写“全球领先”“行业第一”等缺乏依据的结论。
- 对准确率、时延、成本和功耗统一标注“项目素材口径”或“目标指标”。

### 3.2 组件化

- `App.jsx` 只负责页面组合，不堆积所有结构与数据。
- 重复卡片、标题、状态标签、流程节点和动画封装为组件。
- 页面内容数据放入 `src/data/siteData.js` 或同类配置文件。
- 单个组件职责清晰，避免为简单内容建立过度抽象。

### 3.3 视觉一致

- 使用 `design.md` 中的色彩、间距、圆角和动效规范。
- 卡片圆角不超过 8px。
- 不使用白底企业模板风。
- 不使用紫色霓虹、装饰性渐变圆球或与项目无关的素材。
- 不把每个页面区块都做成悬浮卡片。
- 科技蓝负责识别与数据流，青绿负责安全状态，橙色负责风险与重点。

### 3.4 交互可用

- 导航锚点与区块 `id` 保持一致。
- 固定导航不得遮挡锚点标题。
- 移动导航可用键盘关闭，支持 Escape。
- 表单必须有标签、校验、提交反馈和重置逻辑。
- 链接与按钮不能只有视觉效果而没有实际响应。

### 3.5 响应式

- 优先保证 1440×900、1024×768、390×844 三类视口。
- 技术路线在小屏幕改为纵向，不压缩成不可读的横向小字。
- 表格在手机端改为列表或允许局部滚动，不能撑破页面。
- 固定格式元素应定义稳定尺寸，hover 和动态文本不得引发布局跳动。

## 4. 推荐实现顺序

1. 初始化 Vite、React、Tailwind 和基础依赖。
2. 建立主题变量、全局背景、排版和通用容器。
3. 完成导航、Hero 与声音数据流视觉。
4. 完成痛点、产品方案和技术路线。
5. 完成声音事件与智能决策。
6. 完成应用场景、商业模式、研发闭环和发展规划。
7. 完成 CTA、模拟联系表单和页脚。
8. 补充进入视口动画、hover、平滑滚动和移动导航。
9. 执行构建、静态检查和浏览器多视口验收。

## 5. 数据与文案约束

### 允许使用

- 需求中已经给出的公开统计数据。
- 静态模拟事件、设备状态和风险概率。
- SVG、CSS 网格、CSS 声波和图标构成的抽象视觉。

### 必须标注

- 当前识别准确率约 86.5%：项目素材口径。
- 识别准确率目标 ≥ 90%：目标指标。
- 端到端响应 < 2 秒：目标指标。
- 核心硬件成本约 80 元：项目素材口径。
- 功耗 < 2W：目标指标。
- 阈值表：初始工程配置。

### 禁止

- 把目标指标写成已通过权威检测的结果。
- 编造合作单位、专利、获奖、融资或客户案例。
- 使用虚假的实时在线设备数、报警数或增长率。

## 6. 代码规范

- 使用函数组件和 React Hooks。
- 组件名使用 PascalCase，变量与函数使用 camelCase。
- 数据常量使用语义清晰的复数命名。
- 样式优先使用 Tailwind 工具类；复杂重复效果可抽为组件类。
- 避免内联大段 SVG；复杂流程视觉拆分为独立组件。
- 不引入功能重叠的大型 UI 库。
- 只在逻辑不直观处添加简短注释。
- 保证 ESLint 无新增错误。

## 7. 动画规范

- framer-motion 只用于进入视口、错峰出现和轻量 hover。
- 无限动画只用于声波、数据流光点等关键视觉。
- 禁止大幅缩放、持续旋转和强烈弹跳。
- 为 `prefers-reduced-motion` 提供降级方案。
- 动画不能阻碍阅读、点击或滚动。

## 8. 图标与图形

- 优先使用 lucide-react。
- 图标按钮必须提供 `aria-label`。
- 使用图标表达麦克风、波形、AI、盾牌、警报、手机、数据库、校园、养老、商铺和设备等概念。
- 项目技术数据流可使用 SVG path，但不得把整页主要视觉做成不可维护的大型 SVG。

## 9. 验证要求

每次完成重要修改后至少执行：

```powershell
npm run build
```

如果项目配置了 lint：

```powershell
npm run lint
```

最终必须使用浏览器检查：

- 首屏是否在一个视口内展示品牌、定位和核心视觉。
- 1440×900 下布局是否平衡。
- 1024×768 下流程与卡片是否拥挤。
- 390×844 下导航、按钮、表格和流程是否可用。
- 页面是否存在横向滚动、文字遮挡、空白区和控制台错误。
- 声波、流程、锚点、折叠菜单和模拟表单是否正常。

## 10. 完成标准

只有同时满足以下条件才算完成：

- `design.md` 的 11 个模块全部落地。
- 视觉明确呈现 SoundSafe、声音感知、AI 识别和安全预警。
- 技术路线、三道决策保险和 Event 对象表达准确。
- 目标指标口径严谨。
- 响应式与交互均经过实际浏览器验证。
- 构建通过，控制台无阻断性错误。
- 没有遗留占位文案、无效按钮或明显模板痕迹。
