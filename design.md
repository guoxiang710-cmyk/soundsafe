# SoundSafe 单页官网设计说明

## 1. 项目概述

**项目名称：** SoundSafe  
**项目副标题：** 基于声音智能感知的环境安全预警系统  
**核心标语：** 先听见风险，再触发可信预警

SoundSafe 通过低成本声学感知终端与 AI 异常声音识别算法，在不依赖持续视频监控的前提下，对玻璃破碎、呼救、撞击、设备异响、火警声等高价值风险事件进行 2 秒级识别、判断与预警，面向校园宿舍、养老看护、小型商铺、出租屋、社区和机房等轻安防场景，提供可部署、可复制、可扩展的环境安全感知方案。

本网站用于互联网+创新创业大赛、大创答辩和项目路演。页面应兼具技术可信度、创新表达、商业逻辑与落地价值，不使用无法证明的“行业第一”“全球领先”等表述。

## 2. 设计目标

1. 在首屏快速讲清楚“项目是什么、解决什么问题、核心优势是什么”。
2. 用可视化数据流表现“声音采集 → AI 识别 → 智能决策 → 多端预警”。
3. 通过技术路线、Event 决策机制和目标指标建立技术可信度。
4. 通过场景、商业模式、研发闭环和发展规划说明落地与成长空间。
5. 保证 PC、平板和手机端均有完整、顺畅且专业的浏览体验。

## 3. 技术方案

- 框架：React + Vite
- 样式：Tailwind CSS
- 图标：lucide-react
- 动画：framer-motion
- 页面：单页响应式官网
- 数据：静态前端配置，不连接数据库
- 交互：锚点滚动、移动导航、卡片悬浮、流程节点动画、模拟联系表单

建议目录：

```text
src/
├─ components/
│  ├─ layout/
│  │  ├─ Navbar.jsx
│  │  ├─ Section.jsx
│  │  └─ Footer.jsx
│  ├─ hero/
│  │  ├─ Hero.jsx
│  │  ├─ MetricCard.jsx
│  │  └─ SoundFlowVisual.jsx
│  ├─ technology/
│  │  ├─ TechnologyPipeline.jsx
│  │  ├─ PipelineNode.jsx
│  │  └─ DecisionPanel.jsx
│  └─ common/
│     ├─ SectionHeading.jsx
│     ├─ TechCard.jsx
│     ├─ DataBadge.jsx
│     └─ Reveal.jsx
├─ sections/
│  ├─ PainPoints.jsx
│  ├─ ProductSolution.jsx
│  ├─ TechnologyRoute.jsx
│  ├─ SoundEvents.jsx
│  ├─ SmartDecision.jsx
│  ├─ Scenarios.jsx
│  ├─ BusinessModel.jsx
│  ├─ ResearchLoop.jsx
│  ├─ Roadmap.jsx
│  └─ ContactCTA.jsx
├─ data/
│  └─ siteData.js
├─ App.jsx
├─ main.jsx
└─ index.css
```

## 4. 视觉系统

### 4.1 色彩

```text
页面底色        #061426
区块深色        #081B33
层级深蓝        #0B284A
科技蓝          #39BFEF
高亮青          #00C2FF
安全青绿        #00C2A8
状态绿          #3AD29F
预警橙          #FF8A00
提示金          #FFC857
主文字          #F6FBFF
次文字          #A9BCD6
弱化边框        rgba(57, 191, 239, 0.18)
```

橙色只用于风险等级、关键数字、按钮 hover 和高优先级事件。科技蓝与青绿承担主交互和安全状态，避免全页面只使用单一蓝色。

### 4.2 背景

- 使用深蓝渐变、细网格、声波线和低透明度光效构建层次。
- 网格间距建议为 32px 或 40px，透明度不超过 8%。
- 区块间使用背景明暗变化形成节奏，不把每个区块包成悬浮大卡片。
- 玻璃态只用于流程节点、指标卡和重复信息卡，圆角不超过 8px。
- 不使用装饰性渐变圆球、紫色霓虹或纯氛围型大图。

### 4.3 字体与排版

- 中文优先：`Inter, "PingFang SC", "Microsoft YaHei", sans-serif`
- 正文行高：1.65 至 1.8
- 首屏标题：PC 64px 左右，移动端 40px 左右
- 区块标题：PC 36 至 44px，移动端 28 至 32px
- 正文：16 至 18px
- 数据标签和代码字段：使用等宽字体
- 不通过视口宽度连续缩放字号，使用响应式断点切换固定字号
- 字间距保持 0

### 4.4 卡片规范

- 背景：`rgba(8, 27, 51, 0.72)`
- 边框：1px 半透明科技蓝
- 圆角：6px 至 8px
- hover：向上移动 4px，边框切换为青色或预警橙
- 阴影：低透明度蓝色外发光，避免厚重黑色阴影
- 卡片内标题不使用 Hero 级字号

## 5. 页面结构

### 5.1 固定导航

导航项：

- 首页
- 项目方案
- 技术路线
- 声音事件
- 应用场景
- 商业模式
- 发展规划
- 联系团队

交互要求：

- 顶部固定，滚动后增加深色半透明背景和模糊效果。
- 点击后平滑滚动到对应区块。
- 当前区块导航项显示青色下划线或状态点。
- 移动端使用菜单图标打开全宽折叠菜单。
- 菜单展开后锁定背景滚动，点击导航或遮罩后关闭。

### 5.2 Hero 首页

左侧内容：

- SoundSafe 品牌名称
- 基于声音智能感知的环境安全预警系统
- “先听见风险，再触发可信预警”
- 低成本声学感知终端 + AI 异常声音识别 + 多端预警闭环
- 主按钮：查看技术路线
- 次按钮：了解应用场景

核心指标：

| 指标 | 说明 | 标注 |
| --- | --- | --- |
| 2 秒级 | 异常识别与响应 | 端到端响应目标 < 2 秒 |
| 约 80 元 | 核心硬件成本 | 项目素材口径 |
| 6 类 | 核心异常声音事件 | 当前方案范围 |
| 本地优先 | 隐私友好型预警 | 产品策略 |

右侧视觉：

使用 SVG 与 CSS 绘制可循环动画，流程为：

```text
麦克风采集 → 声波特征 → AI 识别 → 风险事件 → 手机/后台预警
```

视觉中心可使用圆形 AI 核心节点，外围连接麦克风、频谱、盾牌、警报和移动端图标。数据线沿路径运动，声波柱持续进行错峰缩放。移动端改为纵向流程，避免节点拥挤。

### 5.3 项目痛点与机会

标题：**为什么需要 SoundSafe？**

四类传统方案对比：

| 方案 | 优势 | 不足 |
| --- | --- | --- |
| 摄像头监控 | 画面直观 | 隐私敏感区域难覆盖，对声音异常不敏感 |
| 人工巡检 | 现场判断灵活 | 夜间和低频事件容易漏检，人力成本高 |
| 单点报警器 | 成本低 | 只能识别单一事件，难以理解复杂声音 |
| 普通拾音设备 | 能采集声音 | 不能主动判断，无法形成实时预警闭环 |

目标场景数据：

- 全国各级各类学校：47.00 万所
- 全国学历教育在校生：28646.50 万人
- 60 岁及以上人口：31031 万人
- 养老机构和设施：40.6 万个
- 养老床位合计：799.3 万张

数据区底部统一标注：**数据来源为公开统计资料，页面仅作项目展示使用。**

### 5.4 产品方案

标题：**从“采集声音”升级为“识别—判断—预警—留痕”**

五个模块按数据流排列：

1. 声音采集终端：USB 麦克风、MEMS 麦克风、麦克风阵列
2. AI 识别模块：MFCC 特征提取，SVM/CNN/YAMNet 分类
3. 决策报警模块：阈值判断、连续确认、事件合并
4. 应用展示端：实时状态、分类结果、历史记录、设备管理
5. 云端与平台模块：多设备管理、远程推送、数据闭环、模型迭代

价值矩阵：

- 用户价值：减少视频隐私压力
- 管理价值：降低人工巡查压力
- 商业价值：低成本试点，软件服务提升收益
- 数据价值：异常样本回流推动模型迭代
- 社会价值：服务校园、养老、社区等公共安全场景

### 5.5 技术路线

标题：**技术路线：从环境声音到安全预警事件**

主链路：

```text
真实环境声音
→ 麦克风采集
→ 2 秒音频窗口 audio_window，shape≈(32000,)
→ MFCC/时频特征，mfcc=(13,T)
→ 模型推理，SVM/CNN/YAMNet，prob=(6,)
→ 智能决策，阈值判断 + 连续确认 + 事件合并
→ Event 事件对象
→ 多端预警，UI/消息/数据库/WAV 片段
```

桌面端使用横向可滚动流程或折线阶梯布局，平板与手机端使用纵向时间轴。每个节点包含序号、图标、名称、输入、处理、输出和关键数据结构。

**声音感知**

- 输入：真实环境连续声音
- 处理：麦克风采样、模数转换、环形缓冲、2 秒窗口切片
- 输出：`audio_window`，16kHz、16bit、单声道，`shape≈(32000,)`

**特征提取**

- 输入：2 秒原始波形
- 处理：归一化、预加重、分帧加窗、FFT、Mel 滤波、Log、DCT
- 输出：MFCC 矩阵 `(13,T)` 或 SVM 向量 `(13,)`

**模型推理**

- 输入：`mfcc_vec` 或 `mfcc_map`
- 处理：SVM/CNN/YAMNet 分类
- 输出：六类概率向量 `prob=[p0,p1,p2,p3,p4,p5]`、`label`、`confidence`

**智能决策**

- 输入：`prob + 历史窗口 + 阈值配置`
- 处理：类别阈值、连续两个窗口确认、短时间事件合并
- 输出：`Event` 或 `None`

**多端预警**

- 输入：结构化 `Event`
- 处理：本地报警、APP/微信/企业消息、后台记录、音频片段保存
- 输出：用户可感知报警、历史记录、复盘证据和反馈数据

节点 hover 时边框高亮并微微上浮。进入视口时依次淡入，流程线带有轻量移动光点。

### 5.6 六类声音事件

标题：**识别六类高价值声音事件**

| 事件 | 风险说明 | 适用场景 | 优先级 |
| --- | --- | --- | --- |
| `normal` 正常环境声 | 作为基础背景类别，降低误报 | 全部场景 | 状态类 |
| `glass_break` 玻璃破碎声 | 可能对应破窗、入侵或意外破损 | 商铺、宿舍、出租屋 | 高 |
| `help_call` 呼救声 | 可能对应跌倒、冲突或紧急求助 | 养老、宿舍、社区 | 高 |
| `collision` 撞击声 | 可能对应跌倒、冲突、物体坠落 | 宿舍、养老、商铺 | 中高 |
| `machine_noise` 设备异响 | 可能对应松动、摩擦或机械故障 | 机房、设备间 | 中 |
| `fire_alarm` 火警声 | 可能对应火灾报警设备触发 | 全部重点场景 | 高 |

高优先级事件使用橙色顶部状态条和警报图标，其他卡片使用青色状态条。

### 5.7 智能决策

标题：**模型概率不是报警，Event 才是安全事件**

三道保险：

1. 类别阈值：不同类别采用不同置信度门槛。
2. 连续确认：要求连续窗口结果一致，降低瞬时噪声误报。
3. 事件合并：短时间内同类结果合并为一个事件，避免重复告警。

初始阈值：

| 类别 | 阈值 | 确认规则 | 等级 |
| --- | ---: | --- | --- |
| 玻璃破碎 | 0.85 | 连续 2 窗同类 + 能量突增 | 高 |
| 呼救声 | 0.80 | 连续 2 窗同类 + 人声相关特征 | 高 |
| 设备异响 | 0.75 | 连续 2 窗同类 + 设备间场景配置 | 中 |
| 撞击声 | 0.78 | 连续 2 窗同类 + 时间窗口合并 | 中高 |
| 火警声 | 0.85 | 单窗高置信或连续 2 窗 | 高 |

提示文案：**阈值为初始工程配置，实际部署需根据场景噪声水平、误报成本和用户容忍度调优。**

Event 字段：

```js
{
  event_id,
  type,
  confidence,
  level,
  timestamp,
  device_id,
  audio_path,
  status,
  feedback
}
```

建议用代码面板与事件状态面板并排展示，移动端上下排列。

### 5.8 应用场景

标题：**适用于隐私敏感、点位分散、预算有限的轻安防场景**

| 场景 | 痛点 | SoundSafe 价值 |
| --- | --- | --- |
| 校园宿舍 | 夜间值守压力大，卧室区域不适合持续视频监控 | 感知呼救、撞击与火警，提供隐私友好的安全补充 |
| 养老看护 | 老人呼救与跌倒撞击不一定能被及时发现 | 提前发现异常声音，辅助护理人员快速响应 |
| 小型商铺 | 夜间无人值守，专业安防成本较高 | 低成本识别破窗、撞击与夜间异常 |
| 出租屋/长租公寓 | 点位分散，租户重视隐私 | 提供非视频的补充式安全感知 |
| 社区治理 | 点位多、预算有限、巡查压力大 | 多设备统一管理，支持远程事件查看 |
| 机房运维 | 设备故障可能先以异响或报警声出现 | 识别设备异响和报警声，辅助提前维护 |

### 5.9 商业模式

标题：**硬件入口 + 软件订阅 + 行业定制 + 平台服务**

中心为 SoundSafe 平台，四周连接：

- 硬件销售：声学感知终端，形成试点与批量设备收入
- 软件订阅：远程推送、事件记录、设备管理、模型更新
- 行业定制：养老、机房、校园场景的阈值、模型、报表定制
- 平台服务：多点位管理、数据闭环和运维服务

PC 端采用中心辐射图，移动端采用四张纵向卡片，保证文字可读性。

### 5.10 研发迭代

标题：**离线训练线 + 在线运行线，形成越用越准的数据闭环**

离线训练线：

```text
数据采集 → 清洗标注 → 特征提取 → 模型训练 → 版本发布
```

在线运行线：

```text
实时采集 → 滑窗分析 → 模型推理 → 决策预警 → 用户反馈
```

反馈闭环：

误报、漏报和人工确认结果回流样本库，用于下一轮模型训练和阈值优化。页面应使用双轨流程线，并用回流箭头明确连接反馈与数据采集。

### 5.11 发展规划

标题：**从演示闭环到平台化环境安全感知服务**

1. 原型验证：USB 麦克风 + 电脑，完成采集—识别—报警闭环。
2. 校园试点：树莓派 + MEMS 麦克风，完成宿舍/机房小范围部署。
3. 场景复制：拓展社区、商铺、养老和出租屋场景。
4. 平台演进：多设备后台、远程推送、模型迭代和风险报表。

使用四阶段时间轴。已完成或当前阶段使用青色，后续阶段使用深蓝边框，目标节点使用橙色强调。

### 5.12 底部 CTA 与联系表单

标题：**让环境安全被“听见”**  
副标题：**SoundSafe 用声音感知补足传统安防盲区**

按钮：

- 查看技术路线
- 了解应用场景
- 联系项目团队

模拟表单字段：

- 姓名
- 单位
- 应用场景
- 联系方式
- 留言

表单提交后不调用后端，显示“需求已记录，项目团队将尽快联系”的成功状态，并保留 2 至 3 秒后自动恢复的交互。输入项需有标签、焦点状态、基本必填校验和错误提示。

## 6. 指标表达规范

以下内容必须显示为“项目素材口径”或“目标指标”，避免被理解为已完成的正式检测结论：

- 当前识别准确率约 86.5%
- 识别准确率目标 ≥ 90%
- 端到端响应目标 < 2 秒
- 核心硬件成本约 80 元
- 功耗目标 < 2W

建议统一使用小型标签：

```text
项目素材口径
目标指标
工程初始配置
```

## 7. 动效规范

- Hero：标题、说明、按钮和指标卡依次淡入，上移距离不超过 20px。
- 声波：使用 CSS keyframes 或 SVG path 动画，周期 1.6 至 2.4 秒。
- 流程节点：进入视口后错峰出现，hover 上浮 4px。
- 数字：可使用短时递增动画，但不制造虚假实时数据效果。
- 区块：淡入即可，避免大幅旋转、弹跳和长时间动画。
- 支持 `prefers-reduced-motion`，用户减少动态效果时关闭循环位移和复杂过渡。

## 8. 响应式规范

### PC：≥ 1200px

- 内容最大宽度 1200 至 1280px。
- Hero 左右双栏。
- 技术路线横向展示。
- 商业模式使用中心辐射布局。

### 平板：768px 至 1199px

- Hero 保持双栏或切换为 5:4 比例。
- 多列卡片由 3 列调整为 2 列。
- 技术路线允许横向滚动，并显示滚动提示。

### 手机：< 768px

- 所有主区块单列展示。
- Hero 流程图位于文字下方。
- 指标卡使用 2×2 网格。
- 技术路线改为纵向时间轴。
- 表格转换为卡片列表，避免横向溢出。
- 按钮文字不得截断，主要按钮可占满容器宽度。

## 9. 可访问性与质量

- 正文与背景对比度满足 WCAG AA。
- 所有按钮、链接和输入框具有清晰焦点态。
- 图标按钮必须有 `aria-label` 和 tooltip。
- 装饰性 SVG 使用 `aria-hidden="true"`。
- 表单错误通过文字和颜色共同表达。
- 页面不应出现横向整体滚动条、文字遮挡或卡片高度跳动。
- 使用语义化 `header`、`nav`、`main`、`section`、`footer`。

## 10. 验收清单

- [ ] 页面包含需求中的 11 个模块。
- [ ] 导航固定、锚点准确、移动菜单可用。
- [ ] Hero 清楚呈现项目名称、定位、标语、简介和四项指标。
- [ ] 声音到预警的数据流可视化完整。
- [ ] 技术路线包含输入、处理、输出和数据结构。
- [ ] 六类声音事件及优先级完整。
- [ ] 智能决策三道保险、阈值表和 Event 字段完整。
- [ ] 所有目标数据带有谨慎口径说明。
- [ ] 联系表单具有模拟提交和校验状态。
- [ ] PC、平板、手机均无文字溢出、遮挡和异常横向滚动。
- [ ] 动画轻量并支持减少动态效果设置。
- [ ] 最终通过构建、浏览器控制台和多视口截图检查。
