# SoundSafe 官网实施技能说明

## 1. 技能名称

**SoundSafe 深色科技型路演官网构建**

## 2. 技能用途

本技能用于指导开发代理在当前项目中完成 SoundSafe 单页响应式官网，覆盖需求理解、视觉系统、组件架构、技术流程可视化、交互实现、响应式适配和浏览器验收。

适用任务：

- 新建或完善 SoundSafe React 官网。
- 将设计需求转换为组件和页面区块。
- 优化深色科技风、AI 安防风视觉。
- 实现声音识别技术路线、事件决策和研发闭环图。
- 修复移动端布局、动画、锚点导航和表单交互。
- 对比赛展示页面进行内容完整性和视觉质量验收。

## 3. 必要输入

执行前需要读取：

- `design.md`：页面设计、内容和验收标准。
- `AGENT.md`：开发约束、代码规范和指标口径。
- 当前项目文件：确认技术栈、已有组件、依赖和用户修改。

如文件内容与用户最新指令冲突，以用户最新指令为准，并同步更新相关文档。

## 4. 核心能力

### 4.1 需求结构化

将长篇项目资料整理为：

- 品牌定位
- 核心指标
- 痛点与机会
- 产品模块
- 技术链路
- 风险事件
- 智能决策
- 应用场景
- 商业模式
- 研发闭环
- 发展规划
- 行动召唤

不得因为页面篇幅长而省略技术细节或商业内容。

### 4.2 深色科技视觉

建立深蓝背景、科技蓝数据流、青绿安全状态和橙色预警状态的多层配色系统。使用细网格、声波、频谱、数据连线和玻璃态信息卡营造技术氛围，同时保持稳重与可读性。

### 4.3 技术流程可视化

准确表达：

```text
环境声音
→ 麦克风采集
→ 2 秒音频窗口
→ MFCC/时频特征
→ SVM/CNN/YAMNet 推理
→ 阈值 + 连续确认 + 事件合并
→ Event
→ 多端预警与留痕
```

每个节点应同时具备输入、处理、输出，不只展示营销化标题。

### 4.4 响应式信息重排

- PC：双栏 Hero、横向流程、中心辐射图。
- 平板：双列卡片、局部横向流程。
- 手机：单列 Hero、纵向时间轴、卡片化表格。

移动端不是简单缩小桌面端，应主动调整信息顺序和图形结构。

### 4.5 可信指标表达

能够区分：

- 已有项目素材数据
- 工程初始配置
- 目标指标
- 公开统计资料

所有指标根据其性质显示相应标签和说明。

## 5. 标准工作流

### 步骤一：勘察项目

1. 查看目录、依赖和 Git 状态。
2. 阅读现有组件、样式与配置。
3. 确认是否已有 Vite、Tailwind、lucide-react、framer-motion。
4. 保留用户已有修改，不覆盖无关文件。

### 步骤二：建立内容模型

把卡片、指标、流程节点、阈值、场景和路线图整理为静态数据数组。组件通过数据映射渲染，避免在 JSX 中重复硬编码相同结构。

建议数据结构：

```js
export const metrics = [];
export const painPoints = [];
export const productModules = [];
export const pipelineSteps = [];
export const soundEvents = [];
export const thresholds = [];
export const scenarios = [];
export const revenueStreams = [];
export const roadmap = [];
```

### 步骤三：搭建视觉基础

1. 配置页面背景、字体、颜色和容器宽度。
2. 创建通用 Section、SectionHeading、TechCard 和 Reveal。
3. 设置焦点态、选择文本颜色、滚动行为与 reduced-motion。
4. 验证基础页面没有横向溢出。

### 步骤四：实现首屏

1. 完成固定导航和移动菜单。
2. 完成品牌、标语、说明、按钮和四项指标。
3. 实现声音到预警的动态流程图。
4. 确保首屏在常见桌面视口内露出下一模块提示。

### 步骤五：实现主体区块

按照以下顺序开发：

1. 痛点与机会
2. 产品方案
3. 技术路线
4. 六类声音事件
5. 智能决策
6. 应用场景
7. 商业模式
8. 研发迭代
9. 发展规划
10. CTA 与联系表单

每完成一个区块，检查文案、数据、响应式和 hover 状态。

### 步骤六：补充交互

- 导航平滑滚动。
- 当前区块高亮。
- 移动菜单打开、关闭和 Escape 操作。
- 流程节点进入视口动画。
- 声波与数据流循环动画。
- 联系表单必填校验和模拟成功反馈。

### 步骤七：验证

1. 执行 lint 和生产构建。
2. 启动本地开发服务器。
3. 使用浏览器分别检查 1440×900、1024×768、390×844。
4. 检查控制台、页面高度、锚点位置和交互状态。
5. 截图检查首屏、技术路线、智能决策和移动端导航。
6. 修复所有文字遮挡、横向滚动、断裂流程线和不可读内容。

## 6. 关键实现建议

### Hero 声波

使用一组高度不同的元素或 SVG path：

- 为每条声波设置不同动画延迟。
- 使用 `transform: scaleY()`，避免改变布局尺寸。
- 动画以中心为变换原点。
- 减少动态效果模式下停止循环。

### 技术路线

每个节点的数据结构建议包含：

```js
{
  id: "feature",
  index: "03",
  title: "特征提取",
  icon: AudioWaveform,
  input: "2 秒原始波形",
  process: ["归一化", "分帧加窗", "FFT", "Mel 滤波", "DCT"],
  output: "MFCC 矩阵 (13,T)",
  code: "mfcc=(13,T)"
}
```

### 风险事件

高风险卡片应通过 `priority` 控制颜色与图标，不要为每个类别重复编写组件。

### 阈值表

桌面端可使用语义化表格；小屏幕通过 CSS 切换为卡片列表，或为表格设置仅局部横向滚动。阈值旁显示“初始工程配置”标签。

### 联系表单

前端模拟逻辑：

1. 检查姓名、联系方式和应用场景。
2. 提交时显示短暂 loading。
3. 显示成功信息。
4. 清空表单或保留用户输入，并明确当前没有真实后端。

## 7. 反模式

执行时避免：

- 把项目做成白底蓝字的通用企业模板。
- 用大段空洞口号替代技术输入、处理和输出。
- 所有区块都使用相同卡片网格，导致页面节奏单一。
- 为了炫技加入影响可读性的强烈动画。
- 在移动端保留过宽流程图和密集表格。
- 使用无法解释来源的实时数据和合作成果。
- 让橙色成为大面积背景，削弱预警语义。
- 使用大型外部图片掩盖产品信息。

## 8. 输出要求

完成开发任务时应交付：

- 可运行的 React + Vite 项目。
- 清晰的组件与静态数据结构。
- 完整的单页中文内容。
- 可用的桌面端与移动端交互。
- 构建和浏览器验收结果。
- 必要时更新 `design.md`、`AGENT.md` 或本文件，使文档与实现保持一致。

## 9. 完成判定

满足以下条件后才能结束任务：

- 用户指定内容全部出现。
- 页面第一眼能识别为 SoundSafe 声音智能安全项目。
- 关键技术链路和 Event 决策机制清楚可信。
- 商业与场景内容不弱于技术内容。
- 三类视口均经过实际检查。
- 没有严重布局问题、控制台错误或失效交互。
